name = "nyxialabo"
main = "labo-router.js"
compatibility_date = "2026-09-15"

# Le même Worker compile Azgaar avant chaque déploiement.
[build]
command = "node scripts/build-azgaar.mjs"

# Même site, même domaine, même Worker.
[assets]
directory = "./"
binding = "ASSETS"
run_worker_first = ["/api/*", "/cartographie/azgaar/*"]

[[kv_namespaces]]
binding = "HUB_CONFIG"
id = "75b93baac0974f8cb8d1f2089eceb4e6"

# NyXia Cartographie dans CE MÊME Worker.
[browser]
binding = "BROWSER"

[[r2_buckets]]
binding = "MAPS"
bucket_name = "nyxia-cartographie"

# Secrets existants du Worker nyxialabo restent inchangés :
# ADMIN_PASSWORD, SESSION_SECRET, OPENROUTER_API_KEY, AIMLAPI_API_KEY, etc.
# Google TTS reste inchangé aussi.
